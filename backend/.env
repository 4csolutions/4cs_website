# Backend Port
PORT=5050

# MongoDB Connection String (fallback to localhost if undefined)
MONGODB_URI=mongodb://127.0.0.1:27017/4cs_website

# JWT Security Token Secret for Admin Dashboard session
JWT_SECRET=4cs_super_secret_session_token

# NodeMailer SMTP credentials for Form Enquiries dispatching
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
CONTACT_EMAIL=syedmujeerhashmi@gmail.com
